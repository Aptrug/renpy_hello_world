default mc_name = ""

define mc = Character("[mc_name]", image="rance", color="#c6f53b")

define flash_white = Fade(0.05, 0.02, 0.3, color="#fff")    # Lightning/holy
define flash_black = Fade(0.2, 0.2, 0.2, color="#000")      # Dark/mystery

# You know, you need a more desriptive name than menuset
default menuset = set()
